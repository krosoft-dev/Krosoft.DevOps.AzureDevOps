variables:
  # pipeline
  poolName: "Azure Pipelines"

  # build docker
  basePath: "$(Build.SourcesDirectory)/$(Build.Repository.Name)"
  remoteBasePath: "/apps"

  # déploiement
  artifactName: "output"
